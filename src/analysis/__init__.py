"""Turning raw run files into the tables and figures the manuscripts use.

Nothing in the papers is typed by hand. Every number is read back from
``results/*/raw_runs/*.json``, so a rerun of the experiments regenerates the
manuscripts' contents and the two can never drift apart.
"""
